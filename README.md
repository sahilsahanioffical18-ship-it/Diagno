# Diagno
Nexuro — A brain tumor signal classification system using Stacked Ensemble Learning (CatBoost + LightGBM) and ANN, trained on 11,500 samples across 5 classes with PCA-based dimensionality reduction, achieving ~74% accuracy.
